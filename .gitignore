# ── Python ────────────────────────────────────────────────────────────────────
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.env

# ── Claude Code settings ──────────────────────────────────────────────────────
.claude/

# ── Raw dataset (too large for git — download from grouplens.org/datasets/movielens/25m) ──
data/

# ── Processed ML data (regenerate: python ml/scripts/preprocess.py) ──────────
ml/data/train.csv
ml/data/val.csv
ml/data/user_features.csv
ml/data/item_features.csv
ml/data/user_id_map.json
ml/data/movie_id_map.json
ml/data/item_genre_vecs.npy

# ── Item embeddings & FAISS indices (regenerate: generate_embeddings.py + build_faiss_index.py) ──
ml/embeddings/*.npy
ml/embeddings/*.index
ml/embeddings/item_meta.csv

# ── Trained model weights (regenerate: train_two_tower.py + train_ranker.py) ──
ml/models/*.pt
ml/models/*.joblib
ml/models/*.txt

# ── Frontend build artifacts ──────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
